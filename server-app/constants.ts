// Setting enums to avoid magic strings
export enum DrawingMode {
    HOVER = 'HOVER',
    DRAW = 'DRAW',
    ERASER = 'ERASER'
}

export enum Direction {
    TOP = 'TOP',
    TOP_RIGHT = 'TOP_RIGHT',
    RIGHT = 'RIGHT',
    BOTTOM_RIGHT = 'BOTTOM_RIGHT',
    BOTTOM = 'BOTTOM',
    BOTTOM_LEFT = 'BOTTOM_LEFT',
    LEFT = 'LEFT',
    TOP_LEFT = 'TOP_LEFT'
}

export enum Dimensions {
    HEIGHT = 30,
    WIDTH = 30
}

export enum Mark {
    STAR = '*',
    BLANK = ' '
}